# 第11题：把“无法处理”的协作材料改成可执行信息

## Issue

**问题：** 空姓名时仍输出问候语。

**环境：** Linux，具体发行版和版本待确认；`sdt-greet` 版本待确认。

**复现命令：**

```bash
sdt-greet --name "______"
```

**期望结果：** 姓名为空时，不输出问候语，并给出明确提示或错误信息。

**实际结果：** 仍输出 `Hello, ______!`，并以退出码 `0` 结束。

## 提交信息

**标题：** 修复空姓名仍输出问候语的问题

**正文：**
空姓名未被正确校验，导致程序将无效输入作为有效姓名并输出问候语。增加姓名为空的校验，阻止无效问候语输出，并返回明确结果。

## 评审意见

**Blocking：** 当前空姓名仍被视为有效输入，可能导致错误数据进入后续流程。建议增加空值校验，并补充空姓名场景的测试用例。

**Suggestion：** 建议明确空姓名时的提示信息和退出码，保持命令行行为一致。
